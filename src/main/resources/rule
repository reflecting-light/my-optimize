SWAP1 ADD MUL AND OR XOR
POP1 ISZERO NOT CALLDATALOAD EXTCODESIZE BLOCKHASH MLOAD SLOAD
POP2 LT GT SLT SGT EQ AND OR XOR BYTE SHA3 ADD MUL SUB DIV SDIV MOD SMOD EXP SIGNEXTEND
POP3 ADDMOD MULMOD
POPD ADDRESS ORIGIN CALLER CALLVALUE CALLDATALOAD COINBASE TIMESTAMP NUMBER DIFFICULTY GASLIMIT CODESIZE GASPRICE PC MSIZE GAS

